# Supabase (optional). The app runs fully local-first without these.
# Copy to .env and fill in, or put the same values in app.json -> expo.extra.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
